# ExternKyra.RO

This is a self-taught project. The intention is to return certain values at given addresses on run-time of your Windows device. On the surface, it is to debug applications and programs running, but further improvement and development could lead to the ability to change values found at given address.

As of the moment, features are restricted to manually mapping the program name/PID and taking values at a specific address.
